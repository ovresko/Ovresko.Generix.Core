<UserControl x:Class="Ovresko.Generix.Core.Pages.Home.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ovresko.Generix.Core.Pages.Home"
             mc:Ignorable="d"  Background="Transparent"
             xmlns:Tx="http://unclassified.software/source/txtranslation"
               TextOptions.TextFormattingMode="Display"
    TextOptions.TextRenderingMode="Auto"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0">
        <!--<Grid.Background>
            <ImageBrush Opacity=".4" 
                        ImageSource="{Binding AppBackground, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid.Background>-->
        <ContentControl HorizontalAlignment="Center"  Margin="50" Content="{Binding MasterPanel, Mode=OneWay,
            UpdateSourceTrigger=PropertyChanged}"/>

        <Image Source="{Binding LogoSrc}"   VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="20" Width="80" />
    </Grid>
</UserControl>
